<?php

/**
 * @file
 * User login disable Module.
 */

/**
 * Implements hook_form_alter().
 */
function user_login_form_alter(&$form, $form_state, $form_id) {
  if ($form_id == 'user_login_form') {
    $form['#access'] = FALSE;
    \Drupal::service('page_cache_kill_switch')->trigger();
  }
}
